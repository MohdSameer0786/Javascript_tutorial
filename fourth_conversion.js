let score = "33aa";

let number_score = String(score);

console.log(typeof number_score);
// note => we can conversion like mention above 
// use another datatype in the field of string but it should be with  first capital letter
console.log(typeof number_score);
